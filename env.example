# Database (Docker PostgreSQL)
DATABASE_URL="postgresql://simplelink:simplelink_dev_password@localhost:5432/simplelink"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-this-to-a-random-secret-in-production"

# Email (Docker MailHog for development - no auth required)
EMAIL_SERVER_HOST="localhost"
EMAIL_SERVER_PORT="1025"
EMAIL_FROM="SimpleLink <no-reply@localhost>"
# EMAIL_SERVER_USER and EMAIL_SERVER_PASSWORD are not needed for MailHog

# Optional: For production email (uncomment and configure)
# EMAIL_SERVER_HOST="smtp.gmail.com"
# EMAIL_SERVER_PORT="587"
# EMAIL_SERVER_USER="your-email@gmail.com"
# EMAIL_SERVER_PASSWORD="your-app-password"
# EMAIL_FROM="SimpleLink <no-reply@yourdomain.com>"
